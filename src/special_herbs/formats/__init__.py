"""Manifest schema + load utilities.

Companion-package precursor — published separately as
``special-herbs-formats`` once Vol. 1 ships. Until then, lives here so
internal release pipeline code has a stable schema source.
"""
